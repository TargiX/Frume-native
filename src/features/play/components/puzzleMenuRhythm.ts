import { spacing } from '../../../theme';

/** Semantic spacing for the menu's text groups, controls, and sections. */
export const PUZZLE_MENU_RHYTHM = {
  titleToDetail: spacing.xs,
  detailToControls: spacing.lg,
  betweenSections: spacing.xxl,
  controlPaddingVertical: spacing.md,
} as const;
